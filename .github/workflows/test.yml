name: Release

on: [push, pull_request]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Run tests
      run: cargo test --verbose
